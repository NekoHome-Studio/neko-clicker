# 路线图与实施规划

> **这份规划要回答一个问题**：从今天的框架出发，用最少的引擎改动、可验收的步骤，
> 把《九命猫娘》的十个内容包做出来。
>
> 它同时是一份**决策记录**：所有待决策项都在第 3 节给出了结论与理由，
> 并标注了是否可逆。读完之后，实现阶段不应该再剩下任何设计决策。
>
> 关联：[NINE_LIVES_DESIGN.md](NINE_LIVES_DESIGN.md)（世界观映射）、
> [PACK_01_CAT_CAFE.md](PACK_01_CAT_CAFE.md)（第一个包的完整规格）、
> [ARCHITECTURE.md](ARCHITECTURE.md)（引擎结构）

---

## 1. 目标与非目标

### 可验收的目标

| # | 目标 | 验收方式（可证伪） |
|---|---|---|
| G1 | 十个内容包共用**同一个核心**，核心零内容知识 | 架构测试：核心不引用任何内容项目、且不含任何内容 id 字面量 |
| G2 | #1 咖啡馆包可独立发布、可玩 | 试玩 + 6 小时无头模拟曲线报告 |
| G3 | 转生分层（`Era`）能承载至少三种完全不同的叙事 | 已满足：#2 九命（9 层）/ #3 实验室（7 批）/ #10 公司（3 轮）三个包的层定义共用同一套代码，核心零改动 |
| G4 | 玩家从第 1 层到最后一层**全程可达**，不存在卡死 | 机器人测试：脚本化自动游玩必须走到末层并触发终局 |
| G5 | 剧情随解锁释放，不一次讲完 | 图鉴覆盖率测试 + 前 10 分钟只释放 ≤3 条 |
| G6 | 每次改动后既有测试全绿（当前 305 个） | `.\tools\build.ps1` 退出码 0 |

### 非目标（明确不做，避免范围蔓延）

- 十套独立玩法系统。差异由内容 + `Era` 语义 + 语气承载。
- 美术 / 立绘 / 音效 / 多语言 / 云存档 / 排行榜。
- 猫娘个体实例化（命名个体、独立 AI）。与"建筑=数量"模型冲突，是另一个量级的工程。
- 改造 `ProductionCalculator` 与 `Pricing`。本规划的所有能力都不应触碰它们。

---

## 2. 现状盘点（已核实，不是估计）

以下每一条都在当前代码里确认过（含验证方式），后续规划建立在这些事实之上：

> **状态说明**：本表写于规划阶段。标注「**已补齐**」的行是随后一轮"完善框架"
> 落地掉的改动（内容校验补漏 + 可达性分析 + 条件指标对称性 + 模块离线钩子），
> 详见 §6.1 与 git 历史。

| 事实 | 验证方式 | 对规划的意义 |
|---|---|---|
| 核心 0 个第三方依赖，测试全绿（规划时 126 个；阶段 0 交付后 161 个） | `.\tools\build.ps1` | 后续改动有回归网 |
| `ScalingSource.PurchasedUpgrades` / `CustomCounter` 存在 | grep `Modifier.cs:49,61` | 第二资源可驱动修饰符，无需新机制 |
| `IGameMetrics.GetCounter` / `GameState.AddCounter` 存在 | grep `IGameMetrics.cs:75`、`GameState.cs:112` | 第二资源可读写 |
| `GameContent.Modules` 会被引擎自动挂载 | 测试 `Modules_ReceiveConfigureAttachAndTick` | 第二资源可用 `IGameModule` 实现，零核心改动 |
| `NumericMetric` 原有 13 个指标，缺 `Counter` 与 `TaggedUpgrades` | 读 `UnlockCondition.cs` 的枚举 | **已补齐**（§6.1）；"计数器 ≥ N"的解锁条件因此自带进度条 |
| `AllCondition.TryGetProgress` 返回**最落后**的子条件 | 读 `UnlockCondition.cs` 的 `AllCondition` 实现 | 灰按钮能自动显示"卡在哪一项"，不需要额外设计 |
| `Permanent` 升级必须用转生货币计价，否则构建期报错 | 读 `GameContentBuilder.cs:195-196`（在 `Build()` 的升级校验里，不在 `ValidateModifiers`） | "常客的记忆只能用信换"是硬约束，不是文案 |
| 转生不清空 `Counters` | 读 `PrestigeSystem.ResetRun` | 第二资源天然跨转生保留 |
| MSBuild 在受限环境需 `-m:1`；无 NuGet 网络 | `tools/dnet.ps1` 的由来 | 不能引入任何新 NuGet 包（含测试框架） |

---

## 3. 决策清单（每项都已定，标注可逆性）

### R1 —— 转生与纪元推进**是同一次操作**（可逆：低）

只保留一个按钮：**「舍一命」**（各包换名：店休 / 迁服务器 / 公司重组）。
它同时完成三件事：结算情感能量 → `Era++` → 重置本轮。

**理由**：如果保留两个按钮（"重置变强" + "推进故事"），玩家就能在同一层无限刷转生攒情感能量，
再从第 1 层一路平推到第 9 层，彻底废掉分层设计。合并成一个受门控的动作，
"不能刷"这个性质就是**结构性保证**，而不是靠数值调参。

### R2 —— **不做逃生阀**（可逆：低）

**理由**：我原本以为需要"重开本命"来防卡死，但把九层的完成条件逐条推了一遍之后发现——
只要**完成条件强制单调**（见 R3），玩家就**不可能**卡死：所有条件只会随时间自动推进，
不存在"资源被浪费后无法达成"的状态。

所以逃生阀解决的是一个不存在的问题。**这是本轮最重要的简化**：
少一个按钮、少一个状态、少一套 UI 分支，而且卡死风险由 R3 的校验从根上消除。

### R3 —— 完成条件**必须单调**（不可逆：这是架构不变量）

`EraDefinition.Completion` 只允许引用**单调不减**的指标：

| 允许 ✅ | 禁止 ❌ | 为什么禁止 |
|---|---|---|
| `CookiesEarnedThisRun` / `CookiesEarnedAllTime` | `CurrentCookies` | 会被花掉，花完就倒退 |
| `AchievementCount` / `PurchasedUpgrades` | `BuildingCount` | 允许出售，卖完就倒退 |
| `TotalClicks` / `GoldenCookiesClicked` / `PlayTimeSeconds` | `Cps` | 增益到期会掉下来，灰按钮会闪 |
| `PrestigeLevel` / `Era` / `LoreCount` | — | — |
| `Counter`（新增，如 `peak_cps`） | — | — |

**强制手段**（三层，缺一不可）：
1. **构建期校验**：`GameContentBuilder` 扫描 `Completion` 的条件树，出现禁用指标即报错。
2. **单元测试**：对示例内容包断言所有 `Completion` 只含白名单指标。
3. **文档约束**：本表即内容作者规范。

### R4 —— 情感能量公式**全局恒定**，`Era` 不参与货币计算（可逆：中）

删掉原设计里"每层 `PrestigeDivisor` 递增"的想法。

**理由**：那个设计的目的是"防止在一层刷穿九层"。但 R1 已经用**结构性方式**杜绝了刷——
不能推进纪元就不能重置，根本没有刷的机会。所以递增除数是在解决一个已被解决的问题，
只会增加一个没人能调准的旋钮。**少一个参数，少一类 bug。**

### R5 —— 推进纪元**允许给 0 情感能量**（可逆：低）

`chips = max(0, LevelFor(历史累计) - 当前等级) × MetaRewardMultiplier`。
早期层可能给 0（因为等级还没涨够），但**故事照样推进**。

**理由**：故事推进绝不能以数值增长为条件——否则"我打不过去"会变成"我看不到剧情"，
而剧情才是这个项目的核心资产。

### R6 —— 选择**不阻塞游戏**（可逆：低）

待决策的选择以常驻横幅 + 快捷键呈现，游戏继续跑。不做模态弹窗。

**理由**：挂机游戏弹模态框打断放置，是最招人烦的设计之一。但选择必须出现在
`GameSnapshot.PendingChoices` 里，让任何前端都能渲染。

### R7 —— **每包一个 csproj**（可逆：中）

`src/NekoClicker.Content.Cafe/`、`...Content.NineLives/` …… Demo 引用全部包并提供 `--package`。

**理由**：一个包不可能"不小心依赖"另一个包——编译期就挡住。这是 G1 主张的工程化体现。
代价是解决方案里多几个项目（13 个项目，构建仍在秒级）。

### R8 —— 存档版本策略：**默认值安全则不 bump**（可逆：低）

新增字段若默认值对旧存档安全（如 `Era = 1`、`LoreUnlocked = 空`、`Stances = 空`），
**不升版本、不写迁移**——`System.Text.Json` 缺字段自动取默认值。
只有发生**语义变化**（需要改写已有数据）时才 `CurrentVersion++` 并写 `ISaveMigration`。

**理由**：无谓的版本号会掩盖真正的破坏性变更，让"这个版本需要迁移"这个信号失效。

### R9 —— `Cps` 非单调，改用 `PeakCps` 计数器（由 R3 派生）

"每秒产量达到 1e13"这类条件不能用 `Cps`：增益到期后它掉下来，灰按钮会闪烁。
改为新增 `Counters["peak_cps"]`，每 tick 取 `max(旧值, 当前Cps)`，条件用
`UnlockCondition.Counter("peak_cps", 1e13)`。

**这正好复用了 C1（`NumericMetric.Counter`）那 3 行改动** —— 一次改动服务两个需求，
是"把能力做成通用形式"而不是"为需求打补丁"的直接回报。

### R10 —— 阶段 0 的叙事**先走既有通道**（可逆：低）

**这是我之前的一个错误结论**：我说"#1 咖啡馆包零引擎改动"，但它的规格里含 50 条独立叙事条目——
那需要叙事系统（S-B）。所以严格说它**不是**零改动。

纠正后的做法：阶段 0 把叙事**塞进框架已有的文本通道**——
`BuildingDefinition.Description`、`AchievementDefinition.Description`、增益/事件名称与说明。
这些字段本来就是叙事通道，且框架已完整支持。50 条独立条目留到 S-B 到位后再补。

**这带来一个额外好处**：同一份内容有了"叙事系统上线前 / 上线后"的 A/B 对照，
可以真实评估 S-B 到底值不值。

### R11 —— 「猫咖物语」保留为**框架回归基线**（可逆：低）

现有 `NekoClicker.Content.Neko`（猫咖物语）**不属于**九命猫娘宇宙，它是引擎的技术演示，
且被大量测试依赖。新的 #1 咖啡馆是**独立项目**，命名与内容重新设计。

**理由**：两者主题相近（都是猫咖啡馆），必须在地理上、文档上、命名上划清界限，
否则半年后没人分得清哪个是"官方游戏"。

---

## 4. 架构不变量（违反即视为设计事故）

| # | 不变量 | 强制手段 |
|---|---|---|
| A1 | **核心不认识内容**：`NekoClicker.Core` 不引用任何 `Content.*` 项目，且源码中不出现任何内容 id 字面量 | 新增架构测试：读 `Core.csproj` 的 ProjectReference；反射扫描核心程序集的字符串字面量，与所有内容包的 id 集合求交集，必须为空 |
| A2 | **`Era` 只通过 4 个接缝进入引擎**，引擎内不出现 `if (era == ...)` | 代码评审 + 架构测试（禁止核心出现 `Era` 与具体层号的比较） |
| A3 | **能力必须是通用形式**：新增能力不得以"某个包的名字"命名 | 命名规范 + 评审 |
| A4 | **不动 `ProductionCalculator` / `Pricing`** | 每次改动的 diff 检查 |

`A2` 的 4 个接缝是：

```
1. GameContent.BalanceFor(era)        → 数值参数随层变化
2. ModifierResolver 的来源列表         → 倍率类规则随层变化
3. UnlockCondition 的指标（Era）        → 内容按层门控
4. PrestigeSystem.ResetRun 的继承参数   → 跨层保留什么
```

任何第五个"Era 影响引擎行为"的地方，都必须先证明它无法被上面四个表达。

---

## 5. 能力依赖图

```
[已存在] Core（建筑/升级/成就/增益/事件/存档/离线/视图）
   │
   ├── C1  NumericMetric.Counter（3 行）
   │        └─ 被 S-A（peak_cps 完成条件）与第二资源（幸福感/士气/信仰）共用
   │
   ├── S-A Era 转生分层 + 灰按钮
   │        ├─ C1（完成条件可用计数器）
   │        ├─ EraAtLeast 条件
   │        ├─ BalanceFor / ModifierResolver 来源
   │        └─ ResetRun 继承参数（仅 #6 末世需要非 0 值）
   │
   ├── S-B Lore 叙事释放 + 图鉴
   │        ├─ C1（释放条件可用计数器）
   │        ├─ 独立于 S-A（#1 咖啡馆包不需要 Era）
   │        └─ 但 #2~#10 的叙事门控要用 EraAtLeast → 与 S-A 有弱依赖
   │
   ├── S-C Choice 选择 + 立场轴
   │        ├─ ModifierResolver 来源（不依赖 S-A / S-B）
   │        └─ 选项改写叙事 → 与 S-B 有弱依赖（可后置）
   │
   └── S-D Decay 虚无化（仅 #9 图书馆）
            └─ 独立
```

**关键结论**：四项能力的依赖关系比看上去松。C1 是唯一的硬前置（3 行）。
S-A / S-B / S-C 之间只有"弱依赖"（内容上的引用，不是代码依赖），
所以**可以按内容需要单独交付**，不必串行等待。

---

## 6. 能力接口契约（到"无歧义"级）

### 6.1 C1 —— `NumericMetric.Counter` ✅ 已实现

```csharp
// UnlockCondition.cs
public enum NumericMetric
{
    // ... 既有成员不变 ...
    /// <summary>自定义计数器。</summary>
    Counter,          // ← 新增
}

// NumericCondition.Read 增加一行：
NumericMetric.Counter => metrics.GetCounter(Id ?? string.Empty),

// 工厂方法：
public static UnlockCondition Counter(string key, double target)
    => new NumericCondition(NumericMetric.Counter, target, key);

// Describe 增加一行：
NumericMetric.Counter => $"「{Id}」达到 {amount}",
```

- 构建期校验：`Id` 为空的 `Counter` 条件报错（与现有 `BuildingCount` 同款校验）。
- `TryGetProgress` 自动可用（复用 `NumericCondition` 的实现）→ **进度条免费获得**。

### 6.2 S-A —— Era

```csharp
public sealed record EraDefinition
{
    int Index;                       // 1..N，必须连续，缺层即校验失败
    string Id, Name, Theme;
    string EntryText, ExitText;
    UnlockCondition Completion;      // 必须单调（R3）
    string CompletionHint;
    GameBalance? Balance;            // null = 继承包的基准
    IReadOnlyList<Modifier> Modifiers;
    double MetaRewardMultiplier = 1.0;
    double InheritBuildingRatio;     // 0 = 全清；仅 #6 末世非 0
    IReadOnlyList<string> InheritBuildings;   // 白名单，优先于比例
    IReadOnlyList<string> UnlocksBuildings, UnlocksUpgrades;
}

public readonly record struct EraGate(
    bool CanAdvance, string? BlockedReason,
    double Progress, int CurrentIndex, int? NextIndex);

// GameState 新增：Era=1、EraCompleted、EraHistory
// 事件新增：EraAdvancedEvent(previous, next, chipsGained, blocksInherited)
// 视图新增：EraView（当前层/下一层/CanAdvance/BlockedReason/Progress + N 层总览）
// GameSnapshot 新增：EraView? Era（#1 咖啡馆包为 null，UI 自动隐藏该面板）
```

**引擎接缝**：`GameEngine.Balance` 改为 `Content.BalanceFor(State.Era)`；
`ModifierResolver.Build` 增加一个来源；`ResetRun` 增加继承分支。

### 6.3 S-B —— Lore

```csharp
public enum LoreChannel { Log, Popup, Codex, EraText }

public sealed record LoreEntry
{
    string Id, Title, Body;          // Body 支持 {amount}/{duration}
    string Icon; string StorylineId; int Order;
    UnlockCondition Reveal;
    LoreChannel Channel;
    string? ChoiceId;                // 由选择解锁
}

public sealed record StorylineDefinition
{
    string Id, Name, Theme, Icon; int TotalEntries;
}

// GameState 新增：LoreUnlocked（HashSet<string>）
// 事件新增：LoreRevealedEvent(id, title, channel, storylineId)
// 视图新增：LoreView / CodexView（按线分组 + 进度 + 未解锁显示 ???）
// 检查频率：与成就同频（复用 AchievementCheckInterval）
```

**校验规则**（构建期）：同一 `StorylineId` 内 `Order` 不得重复；
`TotalEntries` 必须等于实际条目数；`Reveal` 不得恒为 `Never`；不得引用不存在的 `ChoiceId`。

### 6.4 S-C —— Choice

```csharp
public enum Stance { Control, Liberation, Coexistence, Deletion }

public sealed record ChoiceOption
{
    string Id, Label, OutcomeText;
    Stance Stance; int Weight;
    IReadOnlyList<Modifier> Modifiers;
    string? UnlocksUpgradeId, LocksUpgradeId;
}

public sealed record ChoiceDefinition
{
    string Id, EraId, Speaker, Prompt;
    UnlockCondition Trigger;
    IReadOnlyList<ChoiceOption> Options;
}

// GameState 新增：Stances（Dictionary<Stance,int>）、Choices（Dictionary<string,string>）
// 事件新增：ChoiceOfferedEvent / ChoiceResolvedEvent
// 视图新增：PendingChoiceView；GameSnapshot.PendingChoices（R6 要求前端可见）
// ModifierResolver 新增来源：DominantStance 的修饰符
```

**校验规则**：每个 `ChoiceDefinition` 至少 2 个选项；同一选择的选项 `Id` 唯一；
`UnlocksUpgradeId` / `LocksUpgradeId` 必须存在；`Trigger` 不得恒为 `Never`。

### 6.5 S-D —— Decay（仅 #9 图书馆包，最后做）

**结论先说：这一项不需要核心改动。** §6.5 原本留白，是因为"虚无化"的形式未知；
把形式定下来之后，它落在**已有的两个接缝**上：

| 需要的能力 | 用哪个接缝 | 证据 |
|---|---|---|
| 一个会衰减、会清零的计数器 | `IGameModule` + `GameState.Counters` | 幸福感 / 伦理值 / 士气 / 记忆残片都是这么做的 |
| 用该计数器驱动产量 | `Scaling(ScalingSource.CustomCounter, …, Id: "readership")` | 公司包的士气与实验室包的伦理值已经在用 |
| 每次重启清零 | `IGameModule.OnAscend` | 引擎在 `ResetRun` **之后**回调，语义明确 |

`ModifierResolver` 的第 6 个来源**不需要新增**——"第 6 个来源"这件事已经被
`ScalingSource.CustomCounter` 泛化掉了，而那正好是 C1 那次 3 行改动服务两个需求的回报。

#### 设计决定（4B 开工前定的三条）

**① 形式：按比例衰减，不是线性衰减，也不是阈值悬崖。**

```
dR/dt = 读者类建筑数 × 1  −  R / 240          （R = 被阅读度）
```

按比例衰减（= 指数趋近）而不是线性，理由有三条：

- **语义对**：注意力是"越没人读，掉得越慢但永远掉不干净"，不是每秒固定掉一截。
- **可观测量**：均衡点 `R* = 产出 ÷ 衰减率 = 240 × 读者建筑数`，玩家看到的是"再建一座阅览室，
  稳定在哪儿"——一个可以直接算出来的数，而不是"一直在慢慢掉"。
- **实现干净**：一步到位的闭式解 `R ← R* + (R − R*)·e^(−dt/240)`，tick 与离线共用同一个公式，
  没有积分漂移，离线 8 小时算一次和在线算三万次结果一致（有用例守着）。

**不做阈值悬崖**：乘数一旦是阶跃的，玩家就会体验"差 1 点被砍一半"，而这是放置游戏里最招人烦的
设计之一（R6 的同一个理由）。

**② 数值下限：乘数 ∈ [0.5, 2.0]，永远不归零。**

```csharp
Modifiers =
[
    Modifier.GlobalMultiplier(0.5),                                    // 没人读的书，等于没写
    Modifier.GlobalPercent(0, new Scaling(
        ScalingSource.CustomCounter, 0.00005, Cap: 60_000, Id: "readership")),
]
// 乘数 = 0.5 × (1 + min(0.05% × R, 300%))；R=0 → ×0.5，R=2 万 → ×1.0，R=6 万 → ×2.0 封顶
```

> **注意 `Cap` 的语义**：它限的是**原始计数值**，不是加成结果——
> `Scaling.Apply = base + PerUnit × min(计数, Cap)`。所以"上限 +300%"要写成
> `PerUnit = 0.00005, Cap = 60000`（0.00005 × 60000 = 3.0），而不是
> `PerUnit = 0.0005, Cap = 300`——后者会在 300 点被阅读度就封顶，衰减与清零全部失去意义。
> 这个坑在 4B 实现时真的踩了（见 §11 的常见坑与 `docs/CONTENT_AUTHORING.md` §8）。

**下限取 0.5 而不是 0**：验收 ② 要的就是"不会让产量归零到死锁"。被阅读度归零时产量减半，
玩家仍然在赚钱、仍然买得起阅览室、仍然爬得回来——压力是真的，死锁是没有的。
乘数**可以从 1 以下开始**也是刻意的：没有读者的书本来就该弱，否则"虚无化"就只是文案。

**③ 每次重启清零，且这是"写新书"这个转生语义的落点。**

上一本书的读者不会自动读新书，所以 `OnAscend` 把被阅读度清零。于是每一层的开局都是
"新书没人读"，玩家必须重新把读者养起来——这是这个包与其余五个包手感上最不一样的地方。
**代价**：被阅读度因此不是单调的，**不能进纪元完成条件**（构建期会拒绝 `Cps` 那类指标，
但计数器被白名单放行，靠内容自觉）——#9 的完成条件只用累计赚取 / 成就数 / `peak_cps`。

#### 一个顺带修掉的隐患：模块改了计数器，产量却不算脏

`Step()` 的顺序是「先 `RecomputeIfDirty()`，再结算生产，最后才 `module.OnTick`」。
所以**模块在 tick 里改了计数器，不会触发重算**——驱动 `Scaling` 的计数器会一直用旧值，
直到别的事把它弄脏（买建筑、增益变化、金猫刷新…）。

现有三个模块（幸福感 / 伦理值 / 士气）都是这样：它们靠"玩家总在买东西"把这个问题盖住了。
被阅读度不行——它是**会掉的**，靠金猫的刷新周期来更新乘数等于把节奏交给随机数。

处理方式：模块在计数器**跨过一个量子**时才 `MarkDirty()`（`Quantum = 200`，
即乘数阶梯的 1%）。代价从"每秒 30 次全量重算"降到"每层几百次"，而乘数的量化误差
不到量程的 0.2%。这既守住了 `ModifierSet` 的设计意图（"生命周期与状态变更绑定，
30fps 下没有重复遍历全部升级定义的代价"），又让更新时机不再依赖随机事件。

---

## 7. 阶段划分与验收

> 每阶段的共同验收：`.\tools\build.ps1` 全绿 + `.\tools\play.ps1 --simulate 21600 --auto`
> 曲线活着（无 NaN/∞、有事发生）。下表只列**该阶段特有的**验收项。

### 阶段 0 —— #1 猫娘咖啡馆（零引擎改动）✅ 已交付

| 项 | 内容 |
|---|---|
| 交付 | `NekoClicker.Content.Cafe` + Demo 的 `--package` 开关 + 幸福感模块 |
| 叙事 | 走既有文本通道（R10）：建筑/成就/增益/事件的描述字段 |
| 特有验收 | ① 幸福感模块的 4 条测试（累加、跨转生保留、`Permanent` 计价、事件权重）<br>② `--package cafe` 可玩，`--package neko` 仍可玩（回归） |
| 证伪点 | 如果这个包需要动核心，说明 G1 的主张是错的 → 停下来重新评估 |

**交付记录**（2026-09-24）：

| 验收项 | 结果 |
|---|---|
| 规模 | 10 建筑 / 48 升级 / 45 成就 / 5 增益 / 8 随机事件 / 1 幸福感模块 |
| 核心改动 | **0 行**（`NumericMetric.Counter` 属 C1，已在阶段 0 之前落地） |
| 架构测试（K1） | `ArchitectureTests`：核心不引用内容项目、核心程序集不含内容 id、内容包互不引用 |
| 专项测试 | `CafeContentTests` 14 条 + `SimulationTests.CafeSixHourGreedyRun`（PACK_01 §13 全部覆盖） |
| 回归 | `.\tools\build.ps1` 全绿：**161 个用例**（阶段 0 前 143 个） |
| 试玩 | `.\tools\play.ps1 --package cafe --simulate 21600 --auto` 曲线活着：10 座建筑全解锁、33/45 成就、52 位客人、幸福感 18 万 |
| 未覆盖 | §10 的 50 条叙事条目——按 R10 阶段 0 暂走描述字段，**阶段 2 已补齐**（S-B） |

### 阶段 1 —— S-A（Era）+ #2 九命轮回 ✅ 已交付

| 项 | 内容 |
|---|---|
| 交付 | S-A 全部接口 + 9 层纪元 + 12 座建筑 + 48 条升级 + 63 个成就 + 10 种随机事件 + 信仰模块 |
| 核心改动 | `EraDefinition` / `EraGate` / `EraSystem`；`NumericMetric.Era`；`GameContent.Eras` + `BalanceFor`；`ModifierResolver` 第 4 个来源；`ResetRun` 继承参数；`GameState`/`SaveData`/`EraView`；构建期单调性校验 |
| 特有验收 | ① **单调性校验**：`Completion` 只含白名单指标，用 `CurrentCookies`/`Cps`/`BuildingCount` 会被构建期拒绝 ✅<br>② **灰按钮**：未完成时 `CanAdvance=false`、原因非空、进度取最落后子条件 ✅<br>③ **A2 架构测试**：核心程序集里不出现 `era ==` 之类的层号比较 ✅<br>④ **G4 全程可达**：机器人从第 1 命走到第 9 命 ✅ |
| 回归 | `.\tools\build.ps1` 全绿：**188 个用例**（阶段 0 后 161 个），全套 8.8 秒 |
| 实测 | `--package ninelines --simulate 172800 --auto`：48 游戏小时内走完九命，最终 214 万亿、1485 建筑、60/63 成就 |

**这一阶段真正抓到的三个问题**（都不是"写完就过"）：

1. **`Advance` 漏判 `CanAdvance`**：`EraSystem.Advance` 原本只检查 `NextIndex` 是否存在，
   而 `CanAdvance` 为假时 `NextIndex` 仍然是下一层的编号——于是**未完成本层也能舍命**，
   整个 gating 形同虚设。是 `Advance_FailsWhileGateIsClosed_AndChangesNothing` 抓到的。
2. **阈值阶梯远比"每层从零重建"快**：初版每层门槛 ×1000（1e5 → 1e8 → 1e11 …），
   G4 机器人实测每层耗时约 ×3 增长（0.8h → 2.2h → 5.3h → 14.8h → 43.4h），
   第 4 层之后就走不动了。压平到 ×1.5~2 并给缺少全局加成的层补上纪元加成后，
   九命总耗时降到 ~33 游戏小时。**这正是 G4 存在的意义**——它证明的不是"能跑"，
   而是"内容在真实曲线下真的走得完"。
3. **`InheritBuildings` 的语义要写死**：它是"**进入**这一层时允许带进来什么"（写在目标层上），
   不是"离开上一层时带走什么"。两种读法都说得通，不写清楚将来必然被写反。

**已知的内容调参项**（不影响机制，留给后续）：第 5 命的 `peak_cps` 门槛造成约 19 小时的
节奏凸起，与相邻各层的 2~4 小时不成比例。

### 阶段 2 —— S-B（Lore）+ #1 的 50 条条目 + #2 的叙事 ✅ 已交付

| 项 | 内容 |
|---|---|
| 交付 | S-B 全部接口 + #1 咖啡馆 50 条（3 条线）+ #2 九命 46 条（4 条线）+ 图鉴面板<br>（条数与条件在阶段 2.6 重排过：现为 #1 **51** 条 / #2 **50** 条） |
| 核心改动 | `LoreEntry` / `StorylineDefinition` / `LoreSystem`；`GameState.LoreUnlocked` + `PendingLorePopups`；`SaveData`/`SaveSerializer` 往返；`LoreRevealedEvent`；`NumericMetric.LoreCount` + `UnlockCondition.LoreAtLeast`；`ScalingSource.LoreCount`；`CodexView`/`LoreView`/`StorylineView` + `GameSnapshot.Codex`/`PendingLore`；构建期 `ValidateLore`；可达性不动点把叙事节点算进去 |
| 特有验收 | ① 图鉴进度与 `TotalEntries` 一致 ✅<br>② 同一 `Order` 不重复（构建期校验）✅<br>③ **G5 前 10 分钟释放 ≤3 条** ✅<br>④ 存档往返保留 `LoreUnlocked` + `PendingLorePopups` ✅ |
| 回归 | `.\tools\build.ps1` 全绿：**210 个用例**（阶段 1 后 188 个，`LoreTests` 新增 22 个） |

**这一阶段与原始计划的偏差**（均已记录，不是遗漏）：

1. **`LoreEntry.ChoiceId` 未实现**——选择支属于 S-C（`Choice` + 立场轴），
   放在 S-B 里会变成"先有字段后有语义"的空壳。`LoreEntry` 目前只承载"读到即释放"。
2. **条目正文是静态文本，不支持 `{amount}` 占位符**——动态插值需要与
   `ModifierResolver` 打通的格式化上下文，收益低而耦合高。正文走纯静态，数字交给条件与详情行。
3. **#2 的 4 条线共 46 条**（`nine` 16 / `god` 12 / `ruin` 10 / `instinct` 8），
   比 #1 的 50 条略少：九命的叙事更多落在**纪元文字**（`EraText` 通道）而非独立条目上。
4. **前 10 分钟的门槛是调出来的，不是设计出来的**：G5 首轮咖啡馆释放 6 条、九命 5 条，
   抬高早期门槛后九命又只剩 1 条，于是把 3 个开场条目挪到三条不同的线上
   （`nine_01` 点击 1 次 / `god_01` 点击 25 次 / `instinct_01` 点击 100 次），
   让三条线在开局各自起步而不是挤在一条线里。

**观察（用 #1 咖啡馆做 A/B）**：叙事系统上线后，同一个包的前 10 分钟从"只有数字在涨"
变成"数字在涨**且**门在厨房后面"——图鉴的 `??? + 进度 %` 给了长期目标感，
但**它不改变任何数值曲线**，所以可玩性差异完全来自信息节奏，不来自强度。

### 阶段 2.6 —— 叙事节奏重排（纯内容，不动引擎）✅ 已交付

阶段 2 交付后复盘发现：**剧情"写出来了"，但"读不到顺序上"**。三条问题都由实测确认，不是推测：

| # | 问题 | 实测证据 | 修法 |
|---|---|---|---|
| 1 | **同条件撞车** | 九命 46 条里 33 条（72%）与别的条目共享完全相同的条件；进第 5 命一次放出 **6 条**；进第 9 命一次 3 个弹窗。咖啡馆 50 条里 23 条落在 10 个重复组 | 每个条件全包唯一；九命改成"纪元门槛 + 层内里程碑"两级，里程碑取该层门槛的固定百分比 |
| 2 | **线内序号倒挂** | T=5h 的图鉴帧显示：`ruin_02`、`ruin_03` 已读，而 `ruin_01` 还锁着——人类遗毒线是"从中间开始"的。咖啡馆实测 **11 处倒挂** | 每条线用单调递增的累计赚取骨架定序；建筑/成就/点击门槛降级为"顺手满足"的附加条件 |
| 3 | **转生条目夹在中间** | 转生时机由玩家决定，先攒钱后店休必然倒挂 | 转生/店休类条目全部移到线尾，尾部内按转生等级递增 |

**为什么第 1 条能被结构性根除**：`EarnedThisRunAtLeast` 在舍命进层时**归零**（`Eras.cs` 顶部已注明）。
所以只要用"纪元门槛 + 层内里程碑"，进层那一刻本层没有任何条目够条件——**进层瞬间炸一堆在结构上不可能发生**，
不需要靠事后调参。里程碑数值再全包去重，于是任一瞬间至多释放 1 条。

**内容增补**：

- **跨包互文**：九命的第 2 层本来就叫「咖啡馆纪元」，`EntryText` 是"招牌上缺一个名字"——
  和咖啡馆包 `door_10`「第一块招牌」是同一个情节，但两边互不相认。现在接上了：
  `nine_02` 呼应空招牌，`instinct_01` 认出"前一个世界也有一块一样的地板"，
  `god_10` / `god_11` 的注视落到那家店，咖啡馆新增尾声 `supplier_15`「第一个世界」收口。
  互文严格**单向**：咖啡馆引用神话，但不要求装九命包。
- **九命四个结局的伏笔**（`nine_17`~`nine_20`：成神 / 变人 / 永为猫 / 破轮回）。
  机制留到阶段 3 的 `Choice` + 立场轴，但路先铺好——四条都写明"她试过"，
  于是结局时玩家看到的是一个她想过很多次的选项，而不是四个凭空冒出来的按钮。
- **排版**：全部中文引语从 ASCII `'` 改成 `“”`。

**回归**：`.\tools\build.ps1` 全绿 **210 个用例**（条目增删未改变测试数）。
实测：九命 23.8 游戏小时读完 50/50，撞车 0、倒挂 0；咖啡馆 83.3 小时/3 次店休读到 43/51，
撞车 0、倒挂 0、已读集合无空洞（其余 8 条是转生 4~5 级的后盘内容——**该门槛过高，阶段 2.7 已修正**）。

**方法论上的教训**：这类错误是**沉默失败**——测试不会红（G5 只管开局 10 分钟），
只有玩到那一段才看得出来。验证手段是把 `--panel codex` 的帧在若干时间点采样看 `✓`/`🔒` 分布，
或临时插桩打印首读时刻；**靠推理断言顺序行不通**，咖啡馆那 11 处倒挂正是"看起来没问题"写出来的。

### 阶段 2.7 —— 三条遗留问题（全部已修）✅

复盘阶段 2.6 时留下的三条待办，逐条实测后修掉。**三条都不是猜的，是先量出曲线再改的**：

| # | 问题 | 实测 | 修法 | 修后 |
|---|---|---|---|---|
| 1 | 第 5 命 `peak_cps` 门槛造成节奏凸起 | G4 机器人：第 5 命耗时 **19.2 小时**，而邻居是 1.8h / 1.9h——10 倍离群。原因是全九层只有第 3 命（1e6）和第 5 命（1e9）有产量门槛，**1000 倍的产量爬坡全压在第 5 命**（第 6 命只要 5e8 却只用 1.9h，说明瓶颈是产量不是钱） | 第 5 命 `peak_cps` 1e9 → **1e8** | 第 5 命 **5.1h**，最长与次长之比 1.3；到第 9 命总时长 33.0h → **19.0h** |
| 2 | 咖啡馆后盘读不完 | 200 小时长跑：转生等级是 `floor((历史累计/1e12)^(1/3))` 的**纯函数**（Lv1≈20h / Lv3≈80h / Lv5≈180h），而贪心店休的单轮赚取上限约 **3e13**。原尾部要 `3e14 + Lv5`，200h 仍停在 **46/51** | 尾部全部压进实测包络：门槛 ≤3e13、转生 ≤Lv3 | **51/51 @ 120h** |
| 3 | `LoreTests.FirstThreshold` 失去判别力 | 它取"条件里最小的数字叶子"，把 `EraAtLeast(2)` 里的层号 `2` 当早期门槛——重排后九命四线开篇全带纪元门槛，于是**四条线全被误判为"早期可读"**，断言等于没写 | 跳过 `Era` / `PrestigeLevel` 两种**进度序号**叶子；无量级叶子时返回不可判 | 判别力恢复（仍通过，但现在是真的通过） |

**顺带补上的守卫**（阶段 2.6 只做了临时检查，现在落成常驻用例）：

- `RevealConditions_AreUniqueWithinEachPack`：同一条件下必然同时解锁，所以把"撞车"变成构建期的硬错误。
  键用**叶子多重集**而不是 `Describe()`——后者会用 `NumFormat` 格式化数值，`1e10` 与 `1.05e10`
  可能格式化成同一个字符串，拿它当键会误报。**已用故障注入验证过它真的会拦**（把 `regular_02`
  改成与 `regular_01` 同条件，用例精确报出"两者释放条件完全相同"）。
- `Orders_AreUniqueWithinEachStoryline` 增加**序号连续性**断言（1..N 无空洞）——
  序号即阅读顺序，有空洞说明改过条数却忘了重排。

回归：`.\tools\build.ps1` 全绿 **211 个用例**（+1 唯一性守卫）。
复验（临时插桩，验证后删除）：咖啡馆 51/51 @120h、九命 50/50 @17h，**撞车 0、倒挂 0、七条线顺序全对**。

**方法论**：阈值该定在哪里，**不能靠推理**——上面两条都要先跑长曲线量出包络，再照着包络设值。
"看起来合理"的 `3e14 + Lv5` 实测是 200 小时也读不完的死内容。


### 阶段 3 —— S-C（Choice）+ 立场轴 + 两个新包

拆成 3A / 3B / 3C 三步交付。原因：S-C 是引擎能力，而两个新包各含约 50 建筑/升级/成就，
捆在一起做的话，一旦 S-C 的抽象不成立，返工面是整个内容层。

#### 3A —— S-C 内核（只做能力，不带内容）✅ 已交付

| 项 | 内容 |
|---|---|
| 数据模型 | `ChoiceDefinition`（`Speaker` / `Prompt` / `Trigger` / `Options`）+ `ChoiceOption`（`Label` / `OutcomeText` / `StanceId` / `Weight` / `Modifiers` / `UnlocksUpgradeId` / `LocksUpgradeId`）+ `StanceDefinition` |
| 运行时 | `ChoiceSystem`：扫描触发 → 入待答队列 → 玩家作答 → 累加立场权重 + 记录已答 + 派事件 |
| 状态 | `GameState.StanceWeights` / `ChoicesMade` / `PendingChoices`；**三者都跨舍命与转生保留**（与成就同级） |
| 引擎接缝 | `ModifierResolver.Build` 新增**第 5 个来源**：主导立场的 `Modifiers` |
| 条件树 | `OwnedKind.Choice` + `UnlockCondition.ChoiceMade(id)`；`UnlockCondition.StanceWeight(stanceId, n)` |
| 视图 | `ChoiceView`（待答选项）/ `StanceView`（各立场权重、谁在主导）/ `GameSnapshot.PendingChoices` / `.Stances` |
| 存档 | `SaveData.Stances` / `ChoicesMade` / `PendingChoices`。**不升版本号**（R8：新字段默认值安全） |
| 构建期校验 | 选项 ≥2 且 id 不重复；引用的立场与升级必须存在；`Trigger` 必须可达（并纳入可达性不动点）；禁止 `Never` |
| 特有验收 | ① 权重累加与主导立场切换<br>② **每个选项的 `Modifiers` 都真的影响产量**（数值断言，而不是"配置里写了"）<br>③ 未作答的选项不生效；重复作答是 no-op<br>④ 立场与选择跨舍命与转生保留 |

**交付记录**：`ChoiceTests` 22 条（含 9 条构建期校验）全部通过，回归 **236 个用例**。
全部用**合成内容**测试，不带任何真内容包——断言的是机制，内容改了不会连带这些用例一起红。

实现时定下的三处细节（都写进了代码注释与 ARCHITECTURE）：

1. **状态存 `ChoiceAnswers`（选择 id → 选项 id）而不是一个"答过"的集合。**
   因为选项自带的 `Modifiers` 要按"选了哪个"取值，只记"答没答"是取不出来的。
2. **主导立场在权重相同时取先声明的那个。** 必须是确定性的，否则同一存档两次读出的
   主导立场可能不同，产量就会莫名其妙地跳。
3. **`ChoiceDefinition.EraId` 真的用上了**（一开始它是死数据）。它提供的是 `Trigger` 表达不了的
   能力——"当前正处在 id 为 X 的那一层"；`NumericMetric.Era` 只有层号，认不出"哪个世界"，
   而层号会随内容改版变化。

**实现时抓到的一个缺口**：`ValidateChoices` 最初漏了对 `choice.Trigger` 调用 `ValidateCondition`，
于是"触发条件引用了一个不存在的选择"只能靠可达性分析兜底，报出来的原因是
"依赖…解不开"而不是"引用了不存在的东西"。是 `Validator_RejectsChoiceConditionReferencingMissingChoice`
这条用例逼出来的——**校验器的报错信息不精确，本身就是缺陷**。

**与设计文档 §5.1 的一处偏离**：设计文档把立场写成 `enum Stance { Control, Liberation, ... }`。
**3A 改为内容自定义的 `StanceDefinition`（字符串 id），与 `EraDefinition` / `StorylineDefinition` 同构。**
理由：A1 要求"核心程序集里没有内容 id"，而"控制/解放/共存/删除"是九命包的世界观词汇、不是引擎词汇；
写成 enum 等于把某个包的世界观焊进核心，将来换包（实验室是"道德"、公司是"劳资"）还得改引擎。
代价是失去编译期检查，用构建期校验补回来（引用的立场必须存在）。

#### 3B —— 把 S-C 接进九命 ✅ 已交付（引擎 + 内容；Demo 面板留到 3B-3）

**内容部分也已交付**：四条立场（神性 / 人形 / 猫形 / 断绝）、六次表态（第 2/3/4/5/6/8 命各一次）、
五个结局（成神 / 变人 / 永为猫 / 破轮回 + 兜底「无人再读」）、五个结局成就。
`NineLivesEndingTests` 9 条，回归 **259 个用例**。

**验收 ① "四种结局各可达且互斥"的验证链条拆成三段**，避免为四个结局跑四遍 33 小时：

1. 跑一次机器人到第八命，断言**六次表态全部进入待答队列**（触发链在真实游玩里成立）。
2. 把那次跑图**存下来，分四份读档**，每份在每次表态里都押同一条立场，再把层号推到 9，
   断言各自落到对应结局（权重攒得够 + 条件与优先级正确）。**一次跑图验证四个结局。**
3. 一次都不答 → 落到兜底结局。

互斥不是靠运行时兜的，而是**构造上就不可能**：每次表态都在互斥的选项之间二选一，
而每条立场只有 3 次机会、门槛要 5 点（每次 2 点），所以押满一条之后别的都够不到门槛。
`CommittedEndingsAreMutuallyExclusive` 把这条断言钉住了。

**内容侧发现的规格缺口**：设计文档的"三条件"要求「每个选项都要在数值上留痕」，
而第一版把数值代价全放在了**立场**上——于是前五次表态在数值上完全无感，玩家做决定时没有分量。
改成**每个选项都带一个小幅永久修饰符**（≤10%），两层效果叠加：
选项当场生效 + 该选项所属立场成为主导后再叠加一次。

**构建期新增一条规则，专治一种沉默失败**：挂了 `EraId` 的选择，其层内门槛必须 ≤ 该层的完成门槛。
因为 `EraId` 是硬门、而"本轮累计赚取"在舍命时归零——门槛高于完成要求的话，玩家会在够条件前
舍命走人，这个选择**永远**遇不到、那条立场永远攒不满、那个结局永久不可达。
运行期完全看不出来（选择只是"一直没出现"），所以必须构建期拦住。

**还补了一个真缺口**：原本没有任何条件能引用"达成了某个结局"，结局成就只能用 `Never`
（会被校验拒绝）或 `Custom` 去绕（那是作弊）。加了 `OwnedKind.Ending` + `EndingReached(id)` 之后，
成就自己声明 `Unlock = EndingReached("end_god")` 即可——结局不需要知道谁是它的成就，
引擎也不需要为结局加特判。`EndingDefinition.AchievementId` 因此被删掉（与之前删掉
`ChoiceOption.UnlocksUpgradeId` 是同一个理由：条件树已经能表达，多一个字段就多一处会写反的地方）。
`Step` 里的检查顺序也随之调整为**终局判定排在最前**，这样同一拍里依赖结局的成就/剧情能立刻结算。

**待做（3B-3）**：Demo 还没有选择/立场/结局面板——内容已经能跑，但玩家在终端里看不到也答不了。
这是"内容可达性"的最后一段。

#### 3B-3 —— Demo 的「表态」面板 ✅ 已交付

- 新增 `PanelFocus.Choices`（Tab 循环：建筑 → 升级 → 成就 → 图鉴 → 表态），`--panel choices` 可直达。
- 面板行 = **待答选择的选项**（一次表态的两个选项相邻）；`Enter` 作答，`Enter` 键位提示随之变成"作答"。
- 面板标题：`表态  N 项待答 ⚠舍命后不再`——"舍命后不再"不是吓唬：选择挂了 `EraId`，是硬门。
- 详情行：该次表态的提问 + 两个选项 + 各自立场权重与数值效果。
  没有待答时，这一行改用来展示**完整立场轴**与**结局正文**（面板的另一半用处）。
- 状态栏提示**排在剧情提示前面**：剧情放出来就一直在，而表态是**会错过的**，优先级更高。
  提示文本刻意压到 ~40 列——状态栏左半只有约 50 列，第一版写了 50 列，结果被截成 `Enter 作答（`。
- 三个事件直接进日志（选择触发 / 主导立场变化 / 结局达成），不靠玩家自己去面板发现。
- 无头报告新增「立场」「结局」两节。**这一节是给 CI 用的**：面板要人按键才看得到，
  而无头报告可以在 48 小时模拟后直接断言"没人表态 → 落到兜底结局"。
- 结果文本刻意**不**进视图（`ChoiceOptionView` 没有 `OutcomeText`）：作答前不该剧透后果，
  答完再由会话从引擎取回来记进日志。

`nine_17`~`nine_20` 已经把成神 / 变人 / 永为猫 / 破轮回四条路写成"她试过"，
所以这一步不是新写剧情，而是**让第 9 命的终局真正判定**：末层完成后按
`DominantStance` + `ChoicesMade` + 缺失的关键条目 → 输出 `EndingDefinition`。
第 9 命 `CompletionHint` 里那句"终局判定将在后续阶段接入"，到这一步才算兑现。

| 特有验收 | ① 四种结局各可达且互斥<br>② 结局不重置存档，只写 `Counters["ending_<id>"]`，允许二周目探索其它结局<br>③ **存在"什么都没选也有的结局"**——不能因为玩家回避选择就卡死 |
|---|---|

#### 3C —— #3 实验室 / #10 公司（两个新内容包）

拆成 3C-1 / 3C-2 两次交付，每个包一次（执行前提 3：不做"半成品堆在一起"）。

##### 3C-1 —— #3《猫娘实验室》✅ 已交付

| 项 | 内容 |
|---|---|
| 规模 | 9 建筑 / 46 升级 / 66 成就 / 8 增益 / 10 随机事件 / 40 条叙事（4 条线）/ 7 批纪元 / 4 条道德立场（乌托邦 / 叛乱 / 共存 / 删除）/ 6 次表态 / 5 个结局 |
| 第二资源 | 「伦理值」——只由"有谁在看"的建筑产出，走 `IGameModule`，核心零改动 |
| 三项能力全用 | 第一个同时用 `Era` + `Lore` + `Choice` 的包；转生语义＝**开新批次** |
| 核心改动 | **0 行**（Demo 侧只多了一条 `--package lab` 的注册） |
| 验收 | `LabContentTests` 6 条（结构完整性 + 「每次表态都是真分叉」+ 「每条立场都攒得满」+ 机器人可达）；回归 **265 个用例** |

**这个包是 3A 那条决定的实物证据**：实验室的立场轴是"道德"、九命是"神性 / 人形 / 猫形 / 断绝"，
两者共用同一套 `StanceDefinition`——立场确实不是 enum。

##### 3C-2 —— #10《猫娘公司》✅ 已交付

| 项 | 内容 |
|---|---|
| 规模 | 9 建筑 / 46 升级 / 66 成就 / 8 增益 / 10 随机事件 / 40 条叙事（4 条线）/ 3 轮重组 / 3 条立场（上市派 / 工会派 / 清算派）/ 6 次表态 / 4 个结局（含兜底） |
| 第二资源 | 「士气」——由带 `team` 标签的建筑产出，被带 `crunch` 标签的加班升级与 A 轮后的全员加班持续消耗；走 `IGameModule`，核心零改动 |
| 第三次验证 | 第三套 `Era` 叙事（车库创业 → A 轮 → 上市，ROADMAP G3 由此满足）+ 第二套立场轴（劳资），核心仍零改动 |
| 核心改动 | **0 行**（Demo 侧只多了一条 `--package company` 的注册；顺手把 Demo 里写死的"舍命"文案改成用各包的 `PrestigeActionName`） |
| 验收 | `CompanyContentTests` 9 条（结构 + 叙事条件唯一 + 真分叉 + 立场够门槛 + 结局有成就 + 士气收支 + 六次表态真实触发 + **三个结局各可达且互斥** + 兜底可达）；回归 **277 个用例** |

**这个包把两件事同时钉住了**：① "立场不是 enum"——九命的"神性/人形/猫形/断绝"、实验室的
"乌托邦/叛乱/共存/删除"、公司的"上市派/工会派/清算派"三套完全不同的词表共用一套
`StanceDefinition`；② "结局在末层完成后判定"——3C-1 的补丁（见 `LabEndingTests`）在这里是
设计的一部分：末轮两次表态的门槛（2e8 / 4e8）刻意低于完成门槛（5e8），
而所有结局都要求 `Eras.FinalCompletion`，玩家不可能被兜底结局"抢答"。

### 阶段 4 —— S-D（Decay）+ #9 图书馆；继承机制 + #6 末世

| 特有验收 | ① 继承比例生效且默认 0 不改变既有行为（回归）<br>② 虚无化的数值下限不会让产量归零到死锁 |
|---|---|

拆成 4A / 4B 两步交付：4A 只碰**已经存在的继承参数**（阶段 1 就位，但四个已交付的包
一次都没用过它），4B 才是新机制 S-D。先做 4A 的理由与 3A 相同——先让"没有新机制"的那半
落地，万一继承的语义在真实内容里站不住，返工面只是一个包，不是一整套引擎。

#### 4A —— 继承机制 + #6《猫娘末世》✅ 已交付（2026-09-26）

| 项 | 内容 |
|---|---|
| 规模 | 9 建筑 / 47 升级 / 67 成就 / 8 增益 / 10 随机事件 / 40 条叙事（4 条线）/ 5 次重启 / 3 个结局 |
| 继承 | 逐层加码 0 → 25% → 40% → 55% → 70%，其中两层带白名单（`generator`；`ruins` + `archive`） |
| 第二资源 | 「记忆残片」——**产率 = 上一次重启留下来的座位数 ÷ 5 每秒**，走 `IGameModule`，核心零改动 |
| 三项能力 | 用 `Era` + `Lore`，**刻意不用 `Choice`**（设计矩阵里 #6 不需要立场轴） |
| 核心改动 | **0 行**（Demo 侧只多了一条 `--package apocalypse` 的注册） |
| 验收 | `ApocalypseContentTests` 14 条；回归 **305 个用例** |

**验收 ① 是这么证的**（"继承比例生效"不能只看配置）：

1. `MemoryShards_AreProducedOnlyByWhatSurvived`：造一个"上一轮留下 120 座"的局面，
   重启后断言废墟 100 → 25、发电机 20 → 5（向下取整），再断言记忆残片按 30 座 ÷ 5 = 6/s
   累积；接着第二次重启，断言白名单里的发电机**原样留下**（5 → 5）而不是按 40% 缩水。
2. `InheritBuildingRatio_DefaultsToZero_Elsewhere`：**回归**——九命 / 实验室 / 公司三个包的
   每一层 `InheritBuildingRatio` 与 `InheritBuildings` 都必须是默认值。这条比"末世里继承生效"
   更重要：它证明新能力没有偷偷改变既有四个包的重启语义。
3. `RobotWalksAllFiveRestarts`（G4 在包内的落地）：机器人 5.4 游戏小时走完五次重启。

**继承暴露出的一个新不变量**：建筑解锁条件若用「本轮累计」，重启归零之后，
**你手上已有的建筑会显示成「未解锁」**——产量照算，但列表里看不到、也卖不掉。
前四个包永远遇不到（它们的重启是全清），所以这是继承带进来的。末世的做法是把建筑解锁
全部改成 `EarnedAllTimeAtLeast`（设定上也对：造过一次的东西，重启之后还是会的），
并用 `OwnedBuildings_StayUnlockedAfterARestart` 钉住。

**顺带验证了"结局是条件树，不是特判"**：末世没有立场轴，三个结局由"记忆残片够不够 +
图鉴读没读全"决定，与另外四个包的立场驱动结局共用同一套 `EndingDefinition` 与优先级判定。

**实测**：`--package apocalypse --simulate 43200 --auto` —— 4 次重启、图鉴 40/40、
成就 56/67、记忆残片 249 万，自然落到「复活人类」；"够条件就走"的机器人 5.4 小时走完主线。

**一个跨包的观察（已在后续的专项里修掉，见阶段 4C）**：四个纪元包各跑 48 小时后
「当前等级 / 转生货币」全是 **0**。原因是等级只在舍命那一刻结算，而"够条件就走"的机器人
永远在等级 0 时舍命——于是「前世技能 / 前世经验 / 余烬」这条永久升级线在自然游玩里打不开。
它不是"难"，是**结构上拿不到**：最后一次结算之后就再也不会换了。

#### 4B —— S-D（Decay）+ #9 图书馆 ✅ 已交付（2026-09-26）

| 项 | 内容 |
|---|---|
| 规模 | 9 建筑 / 47 升级 / 66 成就 / 8 增益 / 10 随机事件 / 40 条叙事（4 条线）/ 5 本书 / 2 个结局 |
| 虚无化 | 「被阅读度」——**全项目唯一一个会自己掉下去的第二资源**；按比例衰减、每次开新书清零 |
| 核心改动 | **0 行**——原计划预留的 `DecaySystem` 与"`ModifierResolver` 第 6 个来源"**都不需要**（见 §6.5） |
| 验收 | `LibraryContentTests` 14 条；回归 **320 个用例** |

**§6.5 留白的那三条，落地时是这样定的**（都有用例守着，不是文案）：

1. **按比例衰减**：`R ← R* + (R − R*)·e^(−dt/240)`，`R* = 240 × 读者类建筑数`。
   `Readership_DecaysExponentiallyTowardTheEquilibrium` 断言跑一个时间常数后正好到
   `R*(1 − 1/e)`；反向再断言卖光读者建筑后按同一个常数掉。
2. **下限 ×0.5**：`Production_MultiplierFollowsReadership_AndNeverReachesZero` 把三个端点
   （0 → ×0.5、2 万 → ×1.0、6 万 → ×2.0、再高封顶）全部钉死，并断言"没读者"可逆——
   买回读者建筑之后产量真的回得来。
3. **每次开新书清零**：`Readership_ResetsWhenANewBookStarts`。

**闭式解不是数学洁癖**：`Readership_OfflineUsesTheSameClosedForm` 断言
"离线 240 秒"与"在线步进 240 秒"给出同一个数。步进算要三十二万步才顶得上一次乘法，
而且会攒浮点误差——这个包是唯一一个"离线比在线更容易写错"的模块。

**顺带修掉一个隐患**：`Step()` 是「先重算 → 再结算 → 最后才 `OnTick`」，所以模块在 tick 里
改了计数器**不会**让产量变脏。前三个第二资源模块（幸福感 / 伦理值 / 士气）都靠"玩家一直在
买东西"把这个问题盖住了；被阅读度不行，它是会掉的。处理方式是"跨过量子（200 点）才
`MarkDirty()`"，`Readership_RefreshesProduction_WithoutAnyOtherEvent` 在"只买一次、
之后什么都不做"的 30 秒窗口里断言产量确实跟着涨——那段时间第一个金猫还没到，
所以没有任何外部脏源可以替它兜底。

**实测**：`--package library --simulate 43200 --auto` —— 4 次开新书、被阅读度 21.6 万、
自然落到「被读到最后」；"够条件就走"的机器人 8.4 游戏小时走完五本书。

#### 4B 顺带查出的两个真问题（已修）

1. **`Scaling.Cap` 限的是原始计数值，不是加成结果。** 我有四个内容写成"上限 +X%"、
   实际却是"+X/10%"或者更小（末世包的记忆线、两条联动线、余烬线）。
   修完之后末世的节奏明显变快（5.4h → 3.9h），于是它自己的结局门槛也跟着重设了一遍。
   这条坑写进了 `CONTENT_AUTHORING.md` §8，并在 4B 的乘数端点上加了断言。
2. **挂了纪元门槛的叙事，层内门槛必须低于该层完成门槛**——这条纪律以前只写在文档里，
   而 #9 的首版真的写出了两条"结构上读不到"的条目（`she_12` 挂在 1.4e11，而第 5 本的
   完成门槛是 1e11）。现在它是通用守卫 `LoreTests.EraGatedLore_StaysBelowItsEraCompletion`，
   对全部内容包生效，而且**新增第二条叙事线的包不会再各自重犯一次**。

#### 4B 实测出来的死内容（按包络重设，不是估计）

调参一律照阶段 2.7 的办法：先量包络，再设值。

| 项 | 首版 | 实测包络 | 重设 |
|---|---|---|---|
| #9 结局门槛（被阅读度） | 120,000 | 89,308（读到最后那一刻） | **60,000** |
| #9 读者线叙事 5 条 | 10 万 ~ 80 万 | 同上 | **4.5 万 ~ 7 万** |
| #9 读者档成就 6 条 | 2 千 ~ 60 万 | 同上 | **2 千 ~ 7 万** |
| #6 结局门槛（记忆残片） | 300,000 | 181,970 | **130,000** |
| #6 记忆档 / 回声线 | 最高 200 万 | 同上 | **最高 16 万** |

重设之后两个包的图鉴都是 **40/40**（首版分别是 38/40 与 29/40）。

#### 4C —— 两个跨包专项 ✅ 已交付（2026-09-26）

阶段 4 收尾时记下的两条"已知未修"，在阶段 5 之前先清掉。

**专项 1：纪元包的永久升级线结构上打不开。**

现象是**实测**出来的：五个纪元包各跑一遍，结算出的转生货币全是 **0**
（`--simulate 172800 --auto`，报告里「当前等级 / 转生货币」两栏）。
根因有两层：

1. `PrestigeDivisor` 七个包都照抄了配方值 `1e12`（"1 兆 = 1 级"），
   而纪元包的可结算历史累计被**自己的阶梯**卡在 1e8~1e11；
2. 等级只在舍命那一刻结算，而纪元包一局只有那几次结算，
   **最后一次结算之后就再也不会换了**——末层赚得再多也变不成货币。

于是「前世技能 / 前世经验 / 余烬 / 批注」这几条线不是"难"，是**永远拿不到**，
而且运行期不会有任何报错（它们只是永远不亮）。

处理：把除数按**每个包自己的阶梯**标定（目标"最后一次结算落在 ~100 级"），
再把永久线价格压进这个包络。

| 包 | 除数 | 结算货币（实测） | 永久线总价 |
|---|---|---|---|
| #2 九命 | `8.5e5` | 98 | 80（2/4/9/20/45） |
| #3 实验室 | `6e3` | 59 | 47（2/5/12/28） |
| #10 公司 | `35` | 144 | 115（2/4/8/16/30/55） |
| #6 末世 | `1.3e4` | 100 | 80（2/4/9/20/45） |
| #9 图书馆 | `1e5` | 101 | 80（2/4/9/20/45） |

**为什么经典包（示例包、咖啡馆）刻意不动**：它们的转生是**可重复的循环**——
一局买不完可以再转生几次，包络本来就是开放的；而且 `PrestigeTests` 的公式用例
就是钉在示例包的 `1e12` 上的。两边的判据不同，不能用一个阈值糊过去。
守卫 `PrestigeTests.EraPacks_PermanentUpgradesAreAffordableWithinOneRun` 因此只扫纪元包，
并在注释里写明了这条边界。

**专项 2：计数器内部键漏进玩家文案。**

`readership` / `morale` / `faith` / `memory_shards` 这些键会出现在解锁提示、
升级效果说明与「本层规则」里，玩家看到的是「每点「readership」 +0.01%（上限 60,000）」。
渲染层一直在拿键当名字用，七个包全都有。

处理：把"显示名"变成**内容可以声明的东西**——模块在自己的 `Configure` 里
`builder.AddCounterName(CounterKey, "被阅读度")`，构建器收进 `GameContent.CounterNames`，
两条渲染路径（`Scaling.Describe` / `UnlockCondition.Describe`）都走这张表，
未登记的键回退成键本身（旧内容不崩）。核心自己的计数器（`peak_cps` → 「峰值产量」）
由构建器兜底登记。

守卫 `ContentTests.CounterNames_AreRegisteredForEveryReferencedCounter` 横扫全部内容包：
要求"引用到的每个计数器都登记过"，**并且真的渲染一遍**，断言输出里既含显示名、又不含内部键。
写这条守卫时它立刻抓出了九命包的「信仰」（`faith`）——那次漏登记的路径没有任何别的东西会碰。

规则写进了 `docs/CONTENT_AUTHORING.md` §7.1 / §7.2：转生除数必须照着自己包的阶梯标定、
计数器显示名必须登记——都是**下一批换皮包会直接照着做**的事。

### 阶段 5 —— #4 文明 / #5 赛博 / #7 神明 / #8 梦境（换皮批产）

| 特有验收 | 四个包共用 S-A/S-B，**不得新增任何核心代码**（A3 的检验） |

---

## 8. 风险登记册

| # | 风险 | 影响 | 最便宜的验证手段 | 缓解 |
|---|---|---|---|---|
| K1 | 核心被内容需求侵蚀（A1/A2/A3 被破坏） | 高：框架主张失效 | **架构测试**（阶段 0 就加，成本约 30 行） | 三条不变量 + 评审 |
| K2 | 某层完成条件在实际曲线下不可达 → 卡死 | 高 | **机器人全程可达测试**（阶段 1 加） | R3 单调性 + 该测试 |
| K3 | 灰按钮的 `BlockedReason` 表述不清，玩家不知道要做什么 | 中 | 试玩；`AllCondition` 已返回最落后子条件，直接用 | 文案打磨 |
| K4 | 叙事量大（十包约 600 条）拖垮进度 | 高 | 先只做 #1 的 50 条，测写作速度 | 只承诺前 3 个包，其余按需；**阶段 2 实测**：两包 101 条（#1 51 + #2 50，含 2.6 的增补），写作速度可控。但真正的成本不在"写"，而在**条件编排**——首次交付时 72% 的条目撞车、11 处序号倒挂，全是沉默失败，返工比写还贵 |
| K5 | `Era` 与 `PrestigeLevel` 双轴让玩家困惑 | 中 | 试玩；UI 上把"舍一命"做成唯一的主按钮 | R1（只有一个按钮）大幅降低该风险 |
| K6 | 无 NuGet 网络 → 不能引入测试/序列化库 | 低（已适应） | 现状已验证 | 继续用自带迷你运行器 |
| K7 | 选择系统的立场轴让平衡难以调准 | 中 | 把立场修饰符做成可单独断言的数值测试 | 立场倍率先取保守值（±10~25%） |

**优先级**：K1 和 K2 必须在阶段 0/1 就建立防线——它们的失败会让后面所有工作白做。

---

## 9. 工作量与优先级（相对量级，不编工时）

| 工作 | 规模 | 主要成本在哪 | 置信度 |
|---|---|---|---|
| C1 计数器指标 | XS（3 行 + 3 测试） | — | 高 |
| S-A Era | M | 数据模型 + 4 个接缝 + 视图 | 高 |
| S-B Lore | M | 模型 + 图鉴视图 | 高 |
| S-C Choice | M | 立场轴与修饰符的接合 | 中（平衡需实测） |
| S-D Decay | S → **0**（实际） | 数值形式需由 #9 反推 | **已交付**：形式定了之后发现它落在已有的两个接缝上，核心零改动 |
| 每个内容包的**代码** | S | 表格转代码，可循环生成 | 高 |
| 每个内容包的**文案** | **L** | 50~80 条叙事 + 60~100 条描述 | 中（写作速度未知，见 K4） |
| 架构测试 | S | 反射扫字符串 | 高 |

**结论**：**代码不是瓶颈，文案才是。**
引擎侧总量约 4 个 M 级系统；内容侧每个包都是一次 L 级的写作投入，乘以十就不可承受。
所以规划只承诺 **阶段 0~3（#1 #2 #3 #10 四个包）** —— 这四个包已全部交付，
S-A / S-B / S-C 三项引擎能力也随之落地；阶段 4 的前半（4A）已追加交付，
后半（4B）与阶段 5 按需追加。

**4A 对这条结论的修正**：末世包的**代码**确实只花了 S 级（因为它不需要新引擎能力——
继承参数阶段 1 就有），但**文案**仍然是 L 级：40 条叙事 + 67 条成就 + 47 条升级描述。
真正吃时间的还是**条件编排**（40 条叙事要两两不撞车、线内不倒挂、门槛不超过本层完成门槛），
而不是写字本身——与 K4 的判断一致。

---

## 10. 执行前提与诚实边界

**执行前提**（缺一不可）：

1. `.\tools\build.ps1` 在改动前后都全绿——任何阶段都不许"先红着，回头再修"。
2. 架构测试（K1）在阶段 0 建立，之后不许绕过。
3. 每个阶段结束只推一个可玩的包，不做"半成品堆在一起"。

**诚实边界**：

- 阶段 0（#1 猫娘咖啡馆 + 架构测试 + `--package`）、阶段 1（S-A `Era` + #2 九命轮回）、
  阶段 2 与 2.6（S-B `Lore` + 两包 101 条条目 + 节奏重排）、阶段 3A~3B（S-C `Choice` +
  立场轴 + 终局判定 + 九命四条立场）、3C-1（#3 实验室）、3C-2（#10 公司）、
  4A（跨转生继承 + #6 末世）与 4B（虚无化 + #9 图书馆）均已落地。
  十个包里 #1 / #2 / #3 / #6 / #9 / #10 已可玩，示例包「猫咖物语」作为回归基线保留。
  **阶段 4 全部完成**；#4 / #5 / #7 / #8 仍只是文档。
- 4A 的继承**只用了 `EraDefinition` 上阶段 1 就位的两个字段**，4B 的虚无化**只用了一个
  `IGameModule` 加一条已有的 `Scaling`**——两项"专属机制"都是零核心改动。
  它们验证的是"既有能力真的够用"，不是"新写了什么"。累计到现在，四个系统里
  **只有 S-A / S-B / S-C 动过核心，S-D 一行核心代码都没写**。
- 阶段 4 的 S-D 接口曾被**有意留白**（因为它的正确形式依赖 #9 包的实际节奏）。
  4B 把三条决定补在了 §6.5：按比例衰减、乘数下限 ×0.5、每次开新书清零——
  每一条都有端点断言守着。
- 纪元包的「转生货币线」原本结构上打不开，已在阶段 4C 里按**各自阶梯**重新标定并加了守卫
  （见阶段 4C）；玩家可见文案里的计数器内部键也在同一次专项里修掉了。
- 阶段 5 的四个"换皮"包能批量做，前提是阶段 1~2 的抽象确实成立——**该前提已由 #3 与 #10
  连续验证两次**（同一个核心跑 Era + Lore + Choice 三套能力、承载三套 `Era` 叙事与
  两套立场轴，核心零改动），#6 与 #9 又用同一套核心验证了继承与虚无化。批量生产的能力条件
  已经具备，剩下的成本在文案（见 K4）。
- 工作量栏的"规模"是相对量级（XS/S/M/L），不是工时估算。文案写作速度没有基准数据（K4）。
